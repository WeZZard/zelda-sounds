name: Propose Release

on:
  workflow_dispatch:

concurrency:
  group: propose-release-${{ github.repository }}
  cancel-in-progress: false

jobs:
  propose:
    if: github.repository == 'WeZZard/zelda-sounds'
    uses: WeZZard/workflows/.github/workflows/propose-release.yml@v1.0.0
    with:
      version_file: canonical/manifest.json
    secrets:
      RELEASE_TOKEN: ${{ secrets.ZELDA_SOUNDS_RELEASE_TOKEN }}
