name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      tag:
        description: Re-run tag + release + dispatch (e.g. v2.0.5)
        required: true
        type: string
      version:
        description: Plugin version without v prefix (optional; derived from tag)
        required: false
        type: string

concurrency:
  group: release-${{ github.repository }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    if: github.repository == 'WeZZard/zelda-sounds'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
          token: ${{ secrets.ZELDA_SOUNDS_RELEASE_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Detect version bump
        id: meta
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ github.event.inputs.tag }}"
            VERSION="${{ github.event.inputs.version }}"
            if [ -z "$VERSION" ]; then
              VERSION="${TAG#v}"
            fi
            echo "skip=false" >> "$GITHUB_OUTPUT"
            echo "version=$VERSION" >> "$GITHUB_OUTPUT"
            echo "tag=$TAG" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          CURRENT=$(jq -r '.version' canonical/manifest.json)
          PREVIOUS=$(git show HEAD~1:canonical/manifest.json 2>/dev/null | jq -r '.version' || echo "")
          if [ -z "$CURRENT" ] || [ "$CURRENT" = "null" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ "$CURRENT" = "$PREVIOUS" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "skip=false" >> "$GITHUB_OUTPUT"
          echo "version=$CURRENT" >> "$GITHUB_OUTPUT"
          echo "tag=v${CURRENT}" >> "$GITHUB_OUTPUT"

      - name: Build plugin
        if: steps.meta.outputs.skip != 'true'
        run: |
          npm ci
          node build.mjs

      - name: Validate plugin
        if: steps.meta.outputs.skip != 'true'
        run: npx --yes @anthropic-ai/claude-code@latest plugin validate .

      - name: Create tag and release
        if: steps.meta.outputs.skip != 'true'
        env:
          GH_TOKEN: ${{ secrets.ZELDA_SOUNDS_RELEASE_TOKEN }}
        run: |
          set -euo pipefail
          TAG="${{ steps.meta.outputs.tag }}"
          VERSION="${{ steps.meta.outputs.version }}"
          if git ls-remote --exit-code origin "refs/tags/${TAG}" >/dev/null 2>&1; then
            echo "Tag $TAG already exists on remote — skipping tag push"
          else
            git tag "$TAG"
            git push origin "$TAG"
          fi
          gh release view "$TAG" >/dev/null 2>&1 || \
            gh release create "$TAG" --title "Zelda Sounds $VERSION" --generate-notes

      - name: Dispatch catalog sync
        if: steps.meta.outputs.skip != 'true'
        env:
          GH_TOKEN: ${{ secrets.ZELDA_SOUNDS_RELEASE_TOKEN }}
        run: |
          set -euo pipefail
          TAG="${{ steps.meta.outputs.tag }}"
          RELEASE_SHA=$(git ls-remote origin "refs/tags/${TAG}^{}" | awk 'NR==1 {print $1}')
          if [ -z "$RELEASE_SHA" ]; then
            RELEASE_SHA=$(git ls-remote origin "refs/tags/${TAG}" | awk 'NR==1 {print $1}')
          fi
          if [ -z "$RELEASE_SHA" ]; then
            RELEASE_SHA="${{ github.sha }}"
          fi
          RELEASE_ID=$(uuidgen | tr '[:upper:]' '[:lower:]')
          gh api repos/WeZZard/skills/dispatches \
            -f event_type=sync-plugin \
            -f "client_payload[release_id]=$RELEASE_ID" \
            -f "client_payload[plugin]=zelda-sounds" \
            -f "client_payload[version]=${{ steps.meta.outputs.version }}" \
            -f "client_payload[tag]=${TAG}" \
            -f "client_payload[sha]=${RELEASE_SHA}" \
            -f "client_payload[repo]=WeZZard/zelda-sounds"
          echo "Dispatched sync-plugin release_id=$RELEASE_ID sha=$RELEASE_SHA"
